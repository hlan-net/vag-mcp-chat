apiVersion: v1
kind: Service
metadata:
  name: {{ include "vag-mcp-chat.fullname" . }}
  labels:
    {{- include "vag-mcp-chat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vag-mcp-chat.selectorLabels" . | nindent 4 }}
